## 🔬 СТАНДАРТ ТРАНСЛЯЦИИ В MATLAB LIVE CODE (.m)
Этот набор правил обязателен для преобразования любого технического, математического или аналитического текста в формат скрипта MATLAB Live Code. Основная цель — исключить конфликты синтаксиса и обеспечить идеальный рендеринг формул во встроенном редакторе.
## 1. Структура строк и префиксы

* Монолитность строк: Каждый логический элемент текста, заголовок, список или математическое выражение (формула) записывается строго на одной физической строке файла. Разрывы строк (переносы) внутри одного предложения или одной формулы запрещены.
* Обязательный префикс: Каждая строка в текстовом блоке без исключения должна начинаться с префикса %[text] (включая пустые строки, которые превращаются в %[text] ).

## 2. Ограничение формул (Одиночный доллар)

* Строго $...$: Любая математическая переменная, символ, вектор, матрица или формула оборачивается исключительно в одиночные знаки доллара с обеих сторон.
* Запрет на двойной доллар: Использование выключных формул через $$ запрещено, так как парсер текстовых файлов .m выведет их в виде сырого неотрендеренного текста.

## 3. Правило одиночного обратного слэша для LaTeX

* Никакого двойного экранирования: Все управляющие команды LaTeX (такие как \mathbf, \sin, \cos, \sqrt, \alpha, \beta, \Delta, \sum, \text, \cdot, \dots, \mathbb, \partial, \Vert, \ge) внутри знаков доллара пишутся строго с одним обратным слэшем: \.
* Запрет на двойной слэш: Конструкции вида \\mathbf или \\sin ломают рендеринг в Live Editor и отображаются как сырой текст с ошибками компиляции.

## 4. Экранирование служебных символов Markdown внутри формул

* Символ нижнего подчеркивания: При использовании индексов внутри формул символ _ обязательно экранируется одиночным обратным слэшем: \_. Если индекс состоит из нескольких символов, они оборачиваются в фигурные скобки с экранированием первого знака: \_{index}.
* Квадратные скобки: Квадратные скобки [ и ], используемые для обозначения векторов, матриц или элементов массивов внутри формул, всегда экранируются одиночным обратным слэшем: \[ и \].
* Обоснование: Это предотвращает ложное срабатывание разметки (курсив, ссылки) и гарантирует, что символы дойдут до MATLAB в исходном виде.

## 5. Запись матриц и векторов в одну строку

* Многострочные матрицы запрещены: Все матрицы, векторы-столбцы и системы уравнений должны быть приведены к строчной записи.
* Разделители: Элементы внутри одной строки матрицы разделяются запятыми ,, а строки между собой — точкой с запятой ;.
* Пример правильной записи вектора-столбца: $\mathbf{X}=\[x,y,z\]^T$

## 6. Верифицированные эталонные примеры (Примеры из практики)
При генерации кода ориентироваться строго на следующие рабочие конструкции, успешно прошедшие компиляцию в MATLAB:

%[text] $\mathbf{X}=\[x,y,z\]^T$
%[text] $\mathbf{X}\_{si}=\[x\_{si},y\_{si},z\_{si}\]^T,\\quad i=1,\\dots,M$
%[text] $\Delta x\_i=x-x\_{si}$
%[text] $\Delta y\_i=y-y\_{si}$
%[text] $\Delta z\_i=z-z\_{si}$
%[text] $r\_{xy,i}=\\sqrt{\\Delta x\_i^2+\\Delta y\_i^2}$
%[text] $\\rho\_i=\\sqrt{\\Delta x\_i^2+\\Delta y\_i^2+\\Delta z\_i^2}$
%[text] $W=\\text{diag}(\\frac{1}{\\sigma\_{\\alpha 1}^2\\cdot\\rho\_1^2},\\quad\\frac{1}{\\sigma\_{\\beta 1}^2\\cdot\\rho\_1^2},\\quad\\dots,\\quad\\frac{1}{\\sigma\_{\\alpha M}^2\\cdot\\rho\_M^2},\\quad\\frac{1}{\\sigma\_{\\beta M}^2\\cdot\\rho\_M^2})$
%[text] Линейный взвешенный метод (WLLS) требует преобразования нелинейных уравнений связи к виду взвешенной линейной системы нормальных уравнений $H\\cdot\\mathbf{X}=b$, где каждая строка нормируется на СКО промаха на местности $\\sigma\\cdot\\rho\_i$.
%[text] ### 2.1. Аналитический вывод строки горизонтального канала (Азимут $\\alpha\_i$)
%[text] $\\frac{\\sin\\alpha\_i}{\\sigma\_{\\alpha i}\\cdot\\rho\_i}\\cdot x-\\frac{\\cos\\alpha\_i}{\\sigma\_{\\alpha i}\\cdot\\rho\_i}\\cdot y=\\frac{\\sin\\alpha\_i\\cdot x\_{si}-\\cos\\alpha\_i\\cdot y\_{si}}{\\sigma\_{\\alpha i}\\cdot\\rho\_i}$
%[text] $H(2i-1,1)=\\frac{\\sin\\alpha\_i}{\\sigma\_{\\alpha i}\\cdot\\rho\_i}$
%[text] $H(2i-1,2)=\\frac{-\\cos\\alpha\_i}{\\sigma\_{\\alpha i}\\cdot\\rho\_i}$
%[text] $H(2i-1,3)=0$
%[text] $b(2i-1,1)=\\frac{\\sin\\alpha\_i\\cdot x\_{si}-\\cos\\alpha\_i\\cdot y\_{si}}{\\sigma\_{\\alpha i}\\cdot\\rho\_i}$
%[text] $-\\frac{\\cos\\alpha\_i\\sin\\beta\_i}{\\sigma\_{\\beta i}\\cdot\\rho\_i}\\cdot x-\\frac{\\sin\\alpha\_i\\sin\\beta\_i}{\\sigma\_{\\beta i}\\cdot\\rho\_i}\\cdot y+\\frac{\\cos\\beta\_i}{\\sigma\_{\\beta i}\\cdot\\rho\_i}\\cdot z=\\frac{-\\cos\\alpha\_i\\sin\\beta\_i\\cdot x\_{si}-\\sin\\alpha\_i\\sin\\beta\_i\\cdot y\_{si}+\\cos\\beta\_i\\cdot z\_{si}}{\\sigma\_{\\beta i}\\cdot\\rho\_i}$
%[text] $H(2i,1)=\\frac{-\\cos\\alpha\_i\\sin\\beta\_i}{\\sigma\_{\\beta i}\\cdot\\rho\_i}$
%[text] $H(2i,2)=\\frac{-\\sin\\alpha\_i\\sin\\beta\_i}{\\sigma\_{\\beta i}\\cdot\\rho\_i}$
%[text] $H(2i,3)=\\frac{\\cos\\beta\_i}{\\sigma\_{\\beta i}\\cdot\\rho\_i}$
%[text] $b(2i,1)=\\frac{-\\cos\\alpha\_i\\sin\\beta\_i\\cdot x\_{si}-\\sin\\alpha\_i\\sin\\beta\_i\\cdot y\_{si}+\\cos\\beta\_i\\cdot z\_{si}}{\\sigma\_{\\beta i}\\cdot\\rho\_i}$
%[text] $\\mathbf{X}\_{WLLS}=(H^TH)^{-1}H^T\\mathbf{b}$
%[text] Поскольку веса $\\frac{1}{\\sigma\\cdot\\rho\_i}$ уже аналитически интегрированы внутрь строк измерительной матрицы H на этапе линеаризации, functional ковариационная матрица шумов приведенной системы нормальных уравнений становится строго единичной: $R\_{normalized}=I$.
%[text] $K\_{WLLS}=(H^TH)^{-1}H^T\\cdot I\\cdot H(H^TH)^{-1}=(H^TH)^{-1}$
%[text] $\\sigma\_x=\\sqrt{K\_{WLLS}(1,1)},\\quad\\sigma\_y=\\sqrt{K\_{WLLS}(2,2)},\\quad\\sigma\_z=\\sqrt{K\_{WLLS}(3,3)}$

